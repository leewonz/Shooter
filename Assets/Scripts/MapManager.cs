using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapManager : MonoBehaviour {
    GameObject floorObj;
    GameObject wallObj;
    // Use this for initialization
    /*
    void Start () {
        floorObj = GameObject.Find("floor dirt 10x");
        wallObj = GameObject.Find("Wall");

        for (int i = -10; i <= 10; i++)
        {
            for (int j = -10; j <= 10; j++)
            {
                Instantiate(floorObj, new Vector3(i, -1, j), Quaternion.identity);
            }
        }
    }
	*/
	// Update is called once per frame
	void Update () {

	}
}
